#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


#TODO: rewrite these coffee scripts to use coffee_chain in head of this plugin
"use strict"
module.exports = (grunt) ->
  compiler = require('./lib/compiler').init(grunt)

  grunt.registerMultiTask "coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", ->
    options =  @options(
      keyword:    '#= require'
      dirKeyword: '#= require_tree'
      extension:  '.coffee'
      separator:  grunt.util.linefeed

    )

    compiler.proceed(@files, options)


"use strict"
exports.init = (grunt) ->
  exports = {}
  #helper   = require('./helpers').init(grunt)
  snockets = new (require("snockets"))()
  path     = require("path")

  exports.proceed = (files, options) ->
    this.isFileAvaliable(files)
    files.forEach this.prepareList

  exports.showError = (error) ->
    grunt.log.error error

  exports.prepareList = (f) ->
    this.isFileAvaliable(f)

    f.src.forEach (filename) ->
      try
        js = snockets.getConcatenation(
          filename
          async: false
        )

        grunt.file.write path.resolve(f.dest), js
        return true
      catch e
        grunt.log.error e
        return false

  exports.isFileAvaliable = (file) ->
    avaliable = file.length or file.dest
    if not avaliable
      this.showError "You should provide existant files in the Gruntfile"
      return avaliable

  exports

"use strict"
exports.init = (grunt) ->
  exports = {}

  exports.validateFiles = (filepath)->
    file_exists = grunt.file.exists(filepath)
    if not file_exists then grunt.log.warn "Source file \"" + filepath + "\" not found."
    file_exists

  exports.pathToFolder = (filepath) ->
    re = new RegExp("^(.*)[/][^/]*$", "")
    filepath.replace re, "$1" + "/"

  exports.searchRegexp = (keyword) ->
    new RegExp("^"+keyword+"[ ](.*)$", "")

  exports.getValidPathes = (file) ->
    file.src.filter( (filepath) =>
      this.validateFiles(filepath)
    )

  exports.extension = (key, extension) ->
    if key == "file" then extension else "*"+extension

  exports

"use strict"
exports.init = (grunt) ->
  exports = {}
  #helper   = require('./helpers').init(grunt)
  snockets = new (require("snockets"))()
  path     = require("path")

  exports.proceed = (files, options) ->
    this.isFileAvaliable(files)
    files.forEach this.prepareList

  exports.showError = (error) ->
    grunt.log.error error

  exports.prepareList = (f) ->
    this.isFileAvaliable(f)

    f.src.forEach (filename) ->
      try
        js = snockets.getConcatenation(
          filename
          async: false
        )

        grunt.file.write path.resolve(f.dest), js
        return true
      catch e
        grunt.log.error e
        return false

  exports.isFileAvaliable = (file) ->
    avaliable = file.length or file.dest
    if not avaliable
      this.showError "You should provide existant files in the Gruntfile"
      return avaliable

  exports

"use strict"
exports.init = (grunt) ->
  exports = {}

  exports.validateFiles = (filepath)->
    file_exists = grunt.file.exists(filepath)
    if not file_exists then grunt.log.warn "Source file \"" + filepath + "\" not found."
    file_exists

  exports.pathToFolder = (filepath) ->
    re = new RegExp("^(.*)[/][^/]*$", "")
    filepath.replace re, "$1" + "/"

  exports.searchRegexp = (keyword) ->
    new RegExp("^"+keyword+"[ ](.*)$", "")

  exports.getValidPathes = (file) ->
    file.src.filter( (filepath) =>
      this.validateFiles(filepath)
    )

  exports.extension = (key, extension) ->
    if key == "file" then extension else "*"+extension

  exports
