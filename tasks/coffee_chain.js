(function() {
  "use strict";
  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  module.exports = function(grunt) {
    var compiler;
    compiler = require('./lib/compiler').init(grunt);
    return grunt.registerMultiTask("coffeeChain", "grunt's task for concatenating CoffeeScript files that have 'require' directive in correct order", function() {
      var options;
      options = this.options({
        keyword: '#= require',
        dirKeyword: '#= require_tree',
        extension: '.coffee',
        separator: grunt.util.linefeed
      });
      return compiler.proceed(this.files, options);
    });
  };

  "use strict";

  exports.init = function(grunt) {
    var exports, path, snockets;
    exports = {};
    snockets = new (require("snockets"))();
    path = require("path");
    exports.proceed = function(files, options) {
      this.isFileAvaliable(files);
      return files.forEach(this.prepareList);
    };
    exports.showError = function(error) {
      return grunt.log.error(error);
    };
    exports.prepareList = function(f) {
      this.isFileAvaliable(f);
      return f.src.forEach(function(filename) {
        var e, js;
        try {
          js = snockets.getConcatenation(filename, {
            async: false
          });
          grunt.file.write(path.resolve(f.dest), js);
          return true;
        } catch (_error) {
          e = _error;
          grunt.log.error(e);
          return false;
        }
      });
    };
    exports.isFileAvaliable = function(file) {
      var avaliable;
      avaliable = file.length || file.dest;
      if (!avaliable) {
        this.showError("You should provide existant files in the Gruntfile");
        return avaliable;
      }
    };
    return exports;
  };

  "use strict";

  exports.init = function(grunt) {
    var exports;
    exports = {};
    exports.validateFiles = function(filepath) {
      var file_exists;
      file_exists = grunt.file.exists(filepath);
      if (!file_exists) {
        grunt.log.warn("Source file \"" + filepath + "\" not found.");
      }
      return file_exists;
    };
    exports.pathToFolder = function(filepath) {
      var re;
      re = new RegExp("^(.*)[/][^/]*$", "");
      return filepath.replace(re, "$1" + "/");
    };
    exports.searchRegexp = function(keyword) {
      return new RegExp("^" + keyword + "[ ](.*)$", "");
    };
    exports.getValidPathes = function(file) {
      var _this = this;
      return file.src.filter(function(filepath) {
        return _this.validateFiles(filepath);
      });
    };
    exports.extension = function(key, extension) {
      if (key === "file") {
        return extension;
      } else {
        return "*" + extension;
      }
    };
    return exports;
  };

  "use strict";

  exports.init = function(grunt) {
    var exports, path, snockets;
    exports = {};
    snockets = new (require("snockets"))();
    path = require("path");
    exports.proceed = function(files, options) {
      this.isFileAvaliable(files);
      return files.forEach(this.prepareList);
    };
    exports.showError = function(error) {
      return grunt.log.error(error);
    };
    exports.prepareList = function(f) {
      this.isFileAvaliable(f);
      return f.src.forEach(function(filename) {
        var e, js;
        try {
          js = snockets.getConcatenation(filename, {
            async: false
          });
          grunt.file.write(path.resolve(f.dest), js);
          return true;
        } catch (_error) {
          e = _error;
          grunt.log.error(e);
          return false;
        }
      });
    };
    exports.isFileAvaliable = function(file) {
      var avaliable;
      avaliable = file.length || file.dest;
      if (!avaliable) {
        this.showError("You should provide existant files in the Gruntfile");
        return avaliable;
      }
    };
    return exports;
  };

  "use strict";

  exports.init = function(grunt) {
    var exports;
    exports = {};
    exports.validateFiles = function(filepath) {
      var file_exists;
      file_exists = grunt.file.exists(filepath);
      if (!file_exists) {
        grunt.log.warn("Source file \"" + filepath + "\" not found.");
      }
      return file_exists;
    };
    exports.pathToFolder = function(filepath) {
      var re;
      re = new RegExp("^(.*)[/][^/]*$", "");
      return filepath.replace(re, "$1" + "/");
    };
    exports.searchRegexp = function(keyword) {
      return new RegExp("^" + keyword + "[ ](.*)$", "");
    };
    exports.getValidPathes = function(file) {
      var _this = this;
      return file.src.filter(function(filepath) {
        return _this.validateFiles(filepath);
      });
    };
    exports.extension = function(key, extension) {
      if (key === "file") {
        return extension;
      } else {
        return "*" + extension;
      }
    };
    return exports;
  };

}).call(this);

/*
//@ sourceMappingURL=coffee_chain.js.map
*/